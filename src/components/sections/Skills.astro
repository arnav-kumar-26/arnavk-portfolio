---
import { skills } from '../../data/skills';
import SkillPill from './SkillPill.astro';
---

<section id="skills" aria-labelledby="skills-heading" class="py-section">
  <div class="mb-8">
    <h2 id="skills-heading" class="reveal font-display text-h2 tracking-tightest">Skills</h2>
  </div>
  <div class="space-y-8">
    {skills.map((cat) => (
      <div class="space-y-3">
        <h3 class="font-mono text-body uppercase tracking-widest text-text-primary">{cat.category}</h3>
        <div class="flex flex-wrap gap-3">
          {cat.items.map((item) => <SkillPill label={item} />)}
        </div>
      </div>
    ))}
  </div>
</section>
