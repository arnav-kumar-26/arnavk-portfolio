---
interface Props {
  label: string;
}

const { label } = Astro.props as Props;
---

<span class="inline-flex items-center rounded-button border border-border-hairline px-4 py-2 font-mono text-[16px] uppercase tracking-wider text-text-primary bg-background">
  {label}
</span>
